# Optional host port overrides for Docker Compose.
FRONTEND_PORT=5173
BACKEND_PORT=8000

# Required local database credential. Keep the existing password for existing volumes.
POSTGRES_PASSWORD=replace-with-your-local-password
